
#include "StdAfx.h"
#include "hoe3d.h"
#include "object.h"
#include "wood.h"
#include "becher.h"

tStrom::tStrom(float sx,float sy)
{

	/*pos[0] = sx;
	pos[2] = sy;
	pos[1] = Becher::GetMap()->GetHeight(sx,sy);

	model = (iHoeModel*)Becher::GetEngine()->Create("model Strom1");
	if (!model)
		exit(1);

	id_obj = Becher::GetEngine()->RegisterObject(this);*/
}

tStrom::~tStrom()
{
	//Becher::GetEngine()->UnregisterObject(id_obj);
}


